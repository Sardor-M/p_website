import React from 'react';
import styled from 'styled-components';

const BlogContainer = styled.div`
  padding: 2rem;
`;

const Blog: React.FC = () => {
  return (
    <BlogContainer>
      <h1>Blog</h1>
      {/* // content */}
    </BlogContainer>
  );
};

export default Blog;